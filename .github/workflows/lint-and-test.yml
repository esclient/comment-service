name: "lint and test"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

permissions:
  contents: read
  pull-requests: write

jobs:
  lint-and-test:
    uses: esclient/tools/.github/workflows/lint-and-test-python.yml@84db005db60d5ca2a357d1796b8b3648a2898077
    with:
      python-version: "3.13.7"
      source: "commentservice"
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
