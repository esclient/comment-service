name: "lint and test"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened ]

permissions:
  contents: read
  pull-requests: write

jobs:
  lint-and-test:
    uses: esclient/tools/.github/workflows/lint-and-test-python.yml@d3ed2b83a8b318be00d3c211560f62ac40efc2d2
    with:
      python-version: "3.13.7"
      source: "commentservice"
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
